import { Sum } from "../Sum";

test("Sum function should calculate the sum of 2 numbers", () => {
  const result = Sum(3, 4);
  // Assertion
  expect(result).toBe(7);
});

// test case of simple javascript examaple Sum.js
